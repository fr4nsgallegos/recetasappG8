import 'package:flutter/material.dart';

class ContainerAux extends StatelessWidget {
  const ContainerAux({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.all(16),
      height: 200,
      width: 300,
      color: Colors.red,
    );
  }
}
